.claude/
venv/
.venv/
**/.venv/
*.log
node_modules/
build/
__pycache__/
*.py[cod]
dist
vite.config.ts.timestamp-*
.env
.env.*
!.env.example
# Next.js / tooling locals
.env.local
.env*.local
app/web/.env.local
app/web/.env*.local
# Coverage / IDE (optional)
coverage/
.nyc_output/
.cursor/

# Архив legacy (Vite и пр.) — не в репозиторий
/archive/

# Локальные черновики / лендинг (не в репозиторий)
/new_lending/
# Явно: локальный бэкенд (уже покрыто .env.*, оставлено для наглядности)
app/backend/.env_development
**/.next/
web/.next/
*.egg-info/
.pytest_cache/
.mypy_cache/
*.tsbuildinfo

# Локальная копия шаблона nginx для ВМ (не коммитить; в репо есть nginx.conf.example)
/nginx.conf
CLAUDE.md

# Playwright — артефакты прогонов (скриншоты, отчёты, результаты)
app/web/tests/screenshots/
app/web/tests/playwright-report/
app/web/tests/test-results/
/mcp-servers/
.codex/
AGENTS.md
